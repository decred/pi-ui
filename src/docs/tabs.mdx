---
name: Tabs
menu: Components
route: /components/tabs
---

import { useState } from "react";
import { Playground, Props } from "docz";
import { Tabs, Tab } from "../index";

# Tabs

## Properties
<Props of={Tabs} />

## Usage

### Simple Tabs

<Playground>
  {() => {
    const [activeTabIndex, setActiveTabIndex] = useState(0);
    return (
      <>
        <Tabs onSelectTab={setActiveTabIndex} activeTabIndex={activeTabIndex}>
            <Tab label="tab 1" />
            <Tab label="tab 2" />
            <Tab label="tab 3" />
        </Tabs>
      </>
    );
  }}
</Playground>

### Vertical Tabs

<Playground>
  {() => {
    const [activeTabIndex, setActiveTabIndex] = useState(0);
    return (
      <>
        <Tabs vertical onSelectTab={setActiveTabIndex} activeTabIndex={activeTabIndex}>
            <Tab label="What is Politeia?" />
            <Tab label="Proposals?" />
            <Tab label="Censorship" />
        </Tabs>
      </>
    );
  }}
</Playground>

### Tabs with counting

<Playground>
  {() => {
    const [activeTabIndex, setActiveTabIndex] = useState(0);
    return (
      <>
        <Tabs onSelectTab={setActiveTabIndex} activeTabIndex={activeTabIndex}>
            <Tab label="tab 1" count={1} />
            <Tab label="tab 2" count={4} />
        </Tabs>
      </>
    );
  }}
</Playground>

### Tabs with content

<Playground>
  {() => {
    const [activeTabIndex, setActiveTabIndex] = useState(0);
    return (
      <>
        <Tabs onSelectTab={setActiveTabIndex} activeTabIndex={activeTabIndex}>
            <Tab label="tab 1" count={1}>
                <div>tab content 1</div>
            </Tab>
            <Tab label="tab 2" count={4} >
                <div>tab content 2</div>
            </Tab>
        </Tabs>
      </>
    );
  }}
</Playground>