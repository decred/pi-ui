---
name: TextArea
menu: Components
route: /components/textarea
---

import { Fragment } from "react";
import { Playground, Props } from "docz";
import { TextArea, Message } from "../index";

# TextArea

## Properties

<Props of={TextArea} />

## Usage

### Normal

<Playground>
  <TextArea id="statement" placeholder="Statement" name="statement" />
</Playground>

### Error

> Error messages can be disabled by toggling on the `wrapped` prop, so it does not render
> the whitespace below the textarea

<Playground>
  <TextArea
    id="statement2"
    placeholder="Statement"
    name="statement"
    error="Invalid statement"
  />
</Playground>
